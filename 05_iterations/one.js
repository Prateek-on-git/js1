
array = [1,2,3,4,5,6]
console.log(array.length)
for (let mew = 0; mew < array.length; mew++) {
    const element = array[mew];
    if(mew==3){
        //console.log("3 number")
    }
   // console.log(`${element} & ${mew}`)
}


for (let   sdd = 0;   sdd < array.length;   sdd++) {
    const element = array[  sdd];
    
}